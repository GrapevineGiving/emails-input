import { EmailsInputObj, EmailsInputProps } from './types';
export default function EmailsInput(container: Node, { name, list, placeholder, validator, baseClass, onChange, }: EmailsInputProps): EmailsInputObj;
