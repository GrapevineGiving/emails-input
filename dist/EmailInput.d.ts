import { EmailsInputObj, EmailsInputProps } from './type/types';
export default function EmailsInput(container: Node, { name, list, placeholder, cleaner, validator, baseClass, onChange, }: EmailsInputProps): EmailsInputObj;
